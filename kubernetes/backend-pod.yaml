apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    app: ticket-backend
spec:
  containers:
  - name: ticket-backend-container
    image: foobarfarm/ticket-backend:0.1.5
    ports:
    - containerPort: 8080
    env:
    - name: DB_URI
      value: "foo"